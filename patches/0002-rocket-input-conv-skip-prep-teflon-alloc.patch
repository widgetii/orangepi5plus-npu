--- src/gallium/drivers/rocket/rkt_device.h.bak	2026-03-19 19:34:27.309794676 +0300
+++ src/gallium/drivers/rocket/rkt_device.h	2026-03-19 19:41:56.152550769 +0300
@@ -75,6 +75,7 @@
 
    bool pooled;
    bool device_resident; /* BO written once, skip subsequent PREP/FINI */
+   bool cpu_write_only;  /* CPU only writes (no read), skip PREP_BO */
    uint8_t *persistent_map; /* Kept mapped to avoid repeated mmap/munmap */
 };
 
--- src/gallium/drivers/rocket/rkt_device.c.bak	2026-03-19 19:34:27.312027958 +0300
+++ src/gallium/drivers/rocket/rkt_device.c	2026-03-19 19:41:58.868544527 +0300
@@ -76,8 +76,9 @@
 
    pipe_resource_reference(&transfer->resource, prsc);
 
-   /* Skip PREP_BO for device-resident BOs (already synced, no NPU writes pending) */
-   if (!rsc->device_resident) {
+   /* Skip PREP_BO for device-resident BOs (already synced, no NPU writes pending)
+    * and for cpu_write_only BOs (no need to invalidate cache for write-only access) */
+   if (!rsc->device_resident && !rsc->cpu_write_only) {
       struct drm_rocket_prep_bo arg = {0};
       arg.handle = rsc->handle;
       arg.timeout_ns = INT64_MAX;
@@ -108,7 +109,8 @@
    struct rkt_screen *screen = rkt_screen(pctx->screen);
    struct rkt_resource *rsrc = rkt_resource(transfer->resource);
 
-   /* Skip FINI_BO for device-resident BOs */
+   /* Skip FINI_BO for device-resident BOs.
+    * Keep FINI_BO for cpu_write_only BOs — must flush cache to device. */
    if (!rsrc->device_resident && transfer->usage == PIPE_MAP_WRITE) {
       struct drm_rocket_fini_bo arg = {0};
       arg.handle = rsrc->handle;
@@ -312,4 +314,4 @@
    screen->resource_destroy = rkt_resource_destroy;
 
    return screen;
-}
\ No newline at end of file
+}
--- src/gallium/drivers/rocket/rkt_ml.c.bak	2026-03-19 19:34:27.307214604 +0300
+++ src/gallium/drivers/rocket/rkt_ml.c	2026-03-19 20:20:42.530190685 +0300
@@ -302,6 +302,15 @@
    return supported;
 }
 
+static bool
+handle_in_array(const uint32_t *arr, unsigned count, uint32_t handle)
+{
+   for (unsigned j = 0; j < count; j++)
+      if (arr[j] == handle)
+         return true;
+   return false;
+}
+
 struct pipe_ml_subgraph *
 rkt_ml_subgraph_create(struct pipe_context *pcontext,
                        const struct pipe_ml_operation *poperations,
@@ -386,6 +395,14 @@
                     calc_raw_output_size(operation));
    }
 
+   /* Phase 3: Mark graph input tensors as cpu_write_only */
+   util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
+                          operation) {
+      if (find_producer(subgraph, operation->input_index) == NULL) {
+         rkt_get_tensor(subgraph, operation->input_index)->cpu_write_only = true;
+      }
+   }
+
    /* Compile */
    util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
                           operation) {
@@ -393,6 +410,23 @@
       compile_operation(subgraph, operation);
    }
 
+   /* Phase 1: Pre-fill padding for graph input tensors */
+   util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
+                          operation) {
+      if (operation->input_channels > 1 &&
+          operation->input_channels % FEATURE_ATOMIC_SIZE != 0 &&
+          find_producer(subgraph, operation->input_index) == NULL) {
+         struct rkt_resource *input_tensor =
+            rkt_get_tensor(subgraph, operation->input_index);
+         struct pipe_transfer *transfer;
+         uint8_t *map = pipe_buffer_map(pcontext, &input_tensor->base,
+                                        PIPE_MAP_WRITE, &transfer);
+         memset(map, (uint8_t)(operation->input_zero_point - 0x80),
+                input_tensor->bo_size);
+         pipe_buffer_unmap(pcontext, transfer);
+      }
+   }
+
    return &subgraph->base;
 }
 
@@ -449,19 +483,30 @@
                      map[n++] = zero_point;
                }
             }
+         } else if (input_channels == 3 &&
+                    DIV_ROUND_UP(input_channels, FEATURE_ATOMIC_SIZE) == 1) {
+            unsigned n = 0;
+            for (int x = 0; x < input_width; x++) {
+               for (int y = 0; y < input_height; y++) {
+                  map[n]     = input_in[x][y][0] - 0x80;
+                  map[n + 1] = input_in[x][y][1] - 0x80;
+                  map[n + 2] = input_in[x][y][2] - 0x80;
+                  n += FEATURE_ATOMIC_SIZE;
+               }
+            }
          } else {
             unsigned n = 0;
             for (int u = 0; u < DIV_ROUND_UP(input_channels, FEATURE_ATOMIC_SIZE);
                  u++) {
+               unsigned base_ch = u * FEATURE_ATOMIC_SIZE;
+               unsigned ch_count = MIN2(input_channels - base_ch,
+                                        FEATURE_ATOMIC_SIZE);
                for (int x = 0; x < input_width; x++) {
                   for (int y = 0; y < input_height; y++) {
-                     for (int c = 0; c < FEATURE_ATOMIC_SIZE; c++) {
-                        unsigned input_channel = c + u * FEATURE_ATOMIC_SIZE;
-                        if (input_channel < input_channels)
-                           map[n++] = input_in[x][y][input_channel] - 0x80;
-                        else
-                           map[n++] = zero_point - 0x80;
+                     for (int c = 0; c < ch_count; c++) {
+                        map[n + c] = input_in[x][y][base_ch + c] - 0x80;
                      }
+                     n += FEATURE_ATOMIC_SIZE;
                   }
                }
             }
@@ -480,57 +525,74 @@
 
    DBG("Submitting graph\n");
 
-   /* Build submit structures once, reuse on subsequent invokes */
+   /* Phase 4: Single merged job for entire subgraph */
    if (!subgraph->submit_cached) {
       unsigned op_count = util_dynarray_num_elements(&subgraph->operations,
                                                      struct rkt_operation);
-      subgraph->cached_jobs = calloc(op_count, sizeof(struct drm_rocket_job));
-      subgraph->cached_in_handles = calloc(op_count, sizeof(uint32_t *));
-      subgraph->cached_out_handles = calloc(op_count, sizeof(uint32_t *));
-      subgraph->cached_job_count = op_count;
-
-      unsigned job_idx = 0;
+      unsigned total_tasks = 0;
+      util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
+                             operation) {
+         total_tasks += util_dynarray_num_elements(&operation->tasks,
+                                                   struct split_task);
+      }
+      struct drm_rocket_task *tasks = calloc(total_tasks, sizeof(*tasks));
+      unsigned task_idx = 0;
       util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
                              operation) {
-         unsigned num_inputs = operation->add_tensor != -1 ? 2 : 1;
-         uint32_t *in_bo_handles = calloc(num_inputs, sizeof(uint32_t));
-         uint32_t *out_bo_handles = malloc(sizeof(uint32_t));
-
-         in_bo_handles[0] = rkt_get_tensor(subgraph, operation->input_index)->handle;
-         if (operation->add_tensor != -1)
-            in_bo_handles[1] =
-               rkt_get_tensor(subgraph, operation->add_tensor)->handle;
-         out_bo_handles[0] =
-            rkt_get_tensor(subgraph, operation->output_index)->handle;
-
-         unsigned num_tasks =
-            util_dynarray_num_elements(&operation->tasks, struct split_task);
-         struct drm_rocket_task *tasks = calloc(num_tasks, sizeof(*tasks));
-         unsigned task_count = 0;
          util_dynarray_foreach (&operation->tasks, struct split_task, task) {
-            tasks[task_count].regcmd = task->regcfg_addr;
-            tasks[task_count].regcmd_count = task->regcfg_amount;
-            task_count++;
+            tasks[task_idx].regcmd = task->regcfg_addr;
+            tasks[task_idx].regcmd_count = task->regcfg_amount;
+            task_idx++;
          }
+      }
+      /* Collect graph-input-only handles for in_bos and graph-output-only
+       * for out_bos. Intermediate tensors go only in out_bos to avoid
+       * the same BO appearing in both arrays (causes -EALREADY in kernel). */
+      unsigned max_handles = op_count * 3;
+      uint32_t *in_handles = calloc(max_handles, sizeof(uint32_t));
+      uint32_t *out_handles = calloc(max_handles, sizeof(uint32_t));
+      unsigned in_count = 0, out_count = 0;
 
-         subgraph->cached_jobs[job_idx].task_struct_size = sizeof(struct drm_rocket_task);
-         subgraph->cached_jobs[job_idx].in_bo_handles = (uint64_t)(uintptr_t)in_bo_handles;
-         subgraph->cached_jobs[job_idx].in_bo_handle_count = num_inputs;
-         subgraph->cached_jobs[job_idx].out_bo_handles = (uint64_t)(uintptr_t)out_bo_handles;
-         subgraph->cached_jobs[job_idx].out_bo_handle_count = 1;
-         subgraph->cached_jobs[job_idx].tasks = (uint64_t)tasks;
-         subgraph->cached_jobs[job_idx].task_count = task_count;
-
-         subgraph->cached_in_handles[job_idx] = in_bo_handles;
-         subgraph->cached_out_handles[job_idx] = out_bo_handles;
-         job_idx++;
+      /* out_handles: all unique output tensors (includes intermediates) */
+      util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
+                             operation) {
+         uint32_t h = rkt_get_tensor(subgraph, operation->output_index)->handle;
+         if (!handle_in_array(out_handles, out_count, h))
+            out_handles[out_count++] = h;
       }
 
+      /* in_handles: only tensors NOT already in out_handles (graph inputs) */
+      util_dynarray_foreach (&subgraph->operations, struct rkt_operation,
+                             operation) {
+         uint32_t h = rkt_get_tensor(subgraph, operation->input_index)->handle;
+         if (!handle_in_array(out_handles, out_count, h) &&
+             !handle_in_array(in_handles, in_count, h))
+            in_handles[in_count++] = h;
+         if (operation->add_tensor != -1) {
+            h = rkt_get_tensor(subgraph, operation->add_tensor)->handle;
+            if (!handle_in_array(out_handles, out_count, h) &&
+                !handle_in_array(in_handles, in_count, h))
+               in_handles[in_count++] = h;
+         }
+      }
+      subgraph->cached_jobs = calloc(1, sizeof(struct drm_rocket_job));
+      subgraph->cached_jobs[0].task_struct_size = sizeof(struct drm_rocket_task);
+      subgraph->cached_jobs[0].in_bo_handles = (uint64_t)(uintptr_t)in_handles;
+      subgraph->cached_jobs[0].in_bo_handle_count = in_count;
+      subgraph->cached_jobs[0].out_bo_handles = (uint64_t)(uintptr_t)out_handles;
+      subgraph->cached_jobs[0].out_bo_handle_count = out_count;
+      subgraph->cached_jobs[0].tasks = (uint64_t)(uintptr_t)tasks;
+      subgraph->cached_jobs[0].task_count = total_tasks;
+      subgraph->cached_in_handles = calloc(1, sizeof(uint32_t *));
+      subgraph->cached_out_handles = calloc(1, sizeof(uint32_t *));
+      subgraph->cached_in_handles[0] = in_handles;
+      subgraph->cached_out_handles[0] = out_handles;
+      subgraph->cached_job_count = 1;
       subgraph->cached_submit.job_struct_size = sizeof(struct drm_rocket_job);
-      subgraph->cached_submit.jobs = (uint64_t)subgraph->cached_jobs;
-      subgraph->cached_submit.job_count = op_count;
+      subgraph->cached_submit.jobs = (uint64_t)(uintptr_t)subgraph->cached_jobs;
+      subgraph->cached_submit.job_count = 1;
       subgraph->submit_cached = true;
-   }
+      }
 
    ret = drmIoctl(screen->fd, DRM_IOCTL_ROCKET_SUBMIT, &subgraph->cached_submit);
    assert(ret == 0);
@@ -611,7 +673,7 @@
       for (unsigned i = 0; i < subgraph->cached_job_count; i++) {
          free(subgraph->cached_in_handles[i]);
          free(subgraph->cached_out_handles[i]);
-         free((void *)subgraph->cached_jobs[i].tasks);
+         free((void *)(uintptr_t)subgraph->cached_jobs[i].tasks);
       }
       free(subgraph->cached_jobs);
       free(subgraph->cached_in_handles);
--- src/gallium/frontends/teflon/tfl_device.c.bak	2026-03-19 19:34:30.070284832 +0300
+++ src/gallium/frontends/teflon/tfl_device.c	2026-03-19 19:34:47.549876601 +0300
@@ -56,6 +56,12 @@
 
    unsigned *output_tensors;
    unsigned output_count;
+
+   /* Phase 5: Pre-allocated invoke buffers (avoid malloc/free per invoke) */
+   void **input_buffers;
+   bool *input_is_signed;
+   void **output_buffers;
+   bool *output_is_signed;
 };
 
 static struct pipe_resource *
@@ -460,6 +466,13 @@
    free(operation->output_tensors);
 }
 
+static bool
+is_signed_type(TfLiteType type)
+{
+   return type == kTfLiteInt8 || type == kTfLiteInt16 ||
+          type == kTfLiteInt32 || type == kTfLiteInt64;
+}
+
 static void *
 partition_init(TfLiteContext *tf_context, const char *buffer, size_t length)
 {
@@ -514,6 +527,20 @@
    memcpy(tsubgraph->output_tensors, params->output_tensors->data,
           params->output_tensors->size * sizeof(*tsubgraph->output_tensors));
 
+   /* Phase 5: Pre-allocate invoke buffers */
+   tsubgraph->input_buffers = malloc(tsubgraph->input_count * sizeof(void *));
+   tsubgraph->input_is_signed = malloc(tsubgraph->input_count * sizeof(bool));
+   tsubgraph->output_buffers = malloc(tsubgraph->output_count * sizeof(void *));
+   tsubgraph->output_is_signed = malloc(tsubgraph->output_count * sizeof(bool));
+
+   for (unsigned i = 0; i < tsubgraph->input_count; i++)
+      tsubgraph->input_is_signed[i] =
+         is_signed_type(tf_context->tensors[tsubgraph->input_tensors[i]].type);
+
+   for (unsigned i = 0; i < tsubgraph->output_count; i++)
+      tsubgraph->output_is_signed[i] =
+         is_signed_type(tf_context->tensors[tsubgraph->output_tensors[i]].type);
+
    if (unlikely(debug_get_option_debug_teflon() & TEFLON_DEBUG_VERBOSE)) {
       struct timespec time;
       clock_gettime(CLOCK_MONOTONIC, &time);
@@ -547,6 +574,10 @@
    context->ml_subgraph_destroy(context, subgraph);
    free(tsubgraph->input_tensors);
    free(tsubgraph->output_tensors);
+   free(tsubgraph->input_buffers);
+   free(tsubgraph->input_is_signed);
+   free(tsubgraph->output_buffers);
+   free(tsubgraph->output_is_signed);
    free(tsubgraph);
 }
 
@@ -565,35 +596,24 @@
       start = (long)time.tv_sec * 1000 + (long)time.tv_nsec / 1000000;
    }
 
-   void **buffers = malloc(tsubgraph->input_count * sizeof(*buffers));
-   bool *is_signed = malloc(tsubgraph->input_count * sizeof(*is_signed));
-   for (unsigned i = 0; i < tsubgraph->input_count; i++) {
-      TfLiteTensor tf_tensor = tf_context->tensors[tsubgraph->input_tensors[i]];
-
-      buffers[i] = tf_tensor.data.data;
-      is_signed[i] = tf_tensor.type == kTfLiteInt8 ||
-                     tf_tensor.type == kTfLiteInt16 ||
-                     tf_tensor.type == kTfLiteInt32 ||
-                     tf_tensor.type == kTfLiteInt64;
-   }
-   context->ml_subgraph_invoke(context, subgraph, tsubgraph->input_count, tsubgraph->input_tensors, buffers, is_signed);
-   free(buffers);
-   free(is_signed);
-
-   buffers = malloc(tsubgraph->output_count * sizeof(*buffers));
-   is_signed = malloc(tsubgraph->output_count * sizeof(*is_signed));
-   for (unsigned i = 0; i < tsubgraph->output_count; i++) {
-      TfLiteTensor tf_tensor = tf_context->tensors[tsubgraph->output_tensors[i]];
-
-      buffers[i] = tf_tensor.data.data;
-      is_signed[i] = tf_tensor.type == kTfLiteInt8 ||
-                     tf_tensor.type == kTfLiteInt16 ||
-                     tf_tensor.type == kTfLiteInt32 ||
-                     tf_tensor.type == kTfLiteInt64;
-   }
-   context->ml_subgraph_read_output(context, subgraph, tsubgraph->output_count, tsubgraph->output_tensors, buffers, is_signed);
-   free(buffers);
-   free(is_signed);
+   /* Phase 5: Use pre-allocated buffers instead of malloc/free per invoke */
+   for (unsigned i = 0; i < tsubgraph->input_count; i++)
+      tsubgraph->input_buffers[i] =
+         tf_context->tensors[tsubgraph->input_tensors[i]].data.data;
+
+   context->ml_subgraph_invoke(context, subgraph, tsubgraph->input_count,
+                               tsubgraph->input_tensors,
+                               tsubgraph->input_buffers,
+                               tsubgraph->input_is_signed);
+
+   for (unsigned i = 0; i < tsubgraph->output_count; i++)
+      tsubgraph->output_buffers[i] =
+         tf_context->tensors[tsubgraph->output_tensors[i]].data.data;
+
+   context->ml_subgraph_read_output(context, subgraph, tsubgraph->output_count,
+                                    tsubgraph->output_tensors,
+                                    tsubgraph->output_buffers,
+                                    tsubgraph->output_is_signed);
 
    if (unlikely(debug_get_option_debug_teflon() & TEFLON_DEBUG_VERBOSE)) {
       struct timespec time;
